<form method="post" action="{{ path('app_facture_don_delete', {'id': facture_don.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture_don.id) }}">
    <button class="btn">Delete</button>
</form>
