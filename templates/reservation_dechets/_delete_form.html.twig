<form method="post" action="{{ path('app_reservation_dechets_delete', {'id': reservation_dechet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_dechet.id) }}">
    <button class="btn">Delete</button>
</form>
