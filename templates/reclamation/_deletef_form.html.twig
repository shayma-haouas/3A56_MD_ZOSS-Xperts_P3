<form method="post" action="{{ path('app_reclamation_deleteback', {'id': reclamation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
   <br> <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation.id) }}">
    <button class="btn  btn-danger">Supprimer</button>
</form>